// && = kiểm tra xem liệu cả hai trường hợp có đúng
// || = kiểm tra xem liệu có một trong hai trường hợp đúng
// !  = đảo ngược trạng thái logic của toán hạng

// && = check if two conditions are true
// || = check if at least one of two conditions is true
// !  = reverses the logic state of its operand

#include<iostream>
int main(){
//int temp = 14;
//if(temp > 10 && temp <30){
//   std::cout<<"the weather is good!";
//}
//if(temp < 10 || temp > 30){
//    std::cout<<"the weather is bad!";
//}
//else{
//   std::cout<<"the weather is good!";
//}
bool sunny = false;
if(sunny){
    std::cout<<"it's sunny outside";
}
else{
    std::cout<<"it's coudy outside";
}
    return 0;
}