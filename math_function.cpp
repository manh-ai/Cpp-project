#include<iostream>
#include<cmath>
int main(){
    double x = 3;
    double y = 8;
    double z;
// z = std::max(x,y);
// z = std::min(x,y);
// z = pow(3,8);
// z = sqrt(6561);
// z = round(3.14);
// z = ceil (2.1);
// z = floor (3.9);
// z = abs(-3);
    std::cout<<z;
    return 0;
}